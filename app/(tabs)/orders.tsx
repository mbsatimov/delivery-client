import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const OrdersScreen = () => {
  return (
    <SafeAreaView>
      <Text>Orders</Text>
    </SafeAreaView>
  );
};

export default OrdersScreen;
